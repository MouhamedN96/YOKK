# =============================================================================
# YOKK App Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local or any file containing real secrets to git
# =============================================================================

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# The base URL of your application (used for OAuth redirects, passkey verification, etc.)
# In development: http://localhost:3000
# In production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Base URL for API endpoints (usually /api for Next.js apps)
NEXT_PUBLIC_API_BASE_URL=/api

# Relying Party ID for WebAuthn/Passkey authentication
# In development: localhost
# In production: your-domain.com (without protocol or port)
NEXT_PUBLIC_RP_ID=localhost

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from: https://app.supabase.com/project/_/settings/api

# Supabase project URL
# Format: https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anonymous/Public Key (safe for client-side)
# This key is safe to expose in the browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase Service Role Key (SERVER-SIDE ONLY - never expose to client)
# This key has admin privileges - keep it secret!
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =============================================================================
# POWERSYNC CONFIGURATION (Offline-First Sync)
# =============================================================================
# Get these from: https://powersync.journeyapps.com

# PowerSync instance URL for real-time sync
# Format: https://<instance-id>.powersync.journeyapps.com
NEXT_PUBLIC_POWERSYNC_URL=https://your-instance.powersync.journeyapps.com

# =============================================================================
# AI SERVICES CONFIGURATION
# =============================================================================

# Groq API Key - Fast inference for quick AI responses
# Get from: https://console.groq.com/keys
# Used for: Speed-critical AI tasks, initial responses
GROQ_API_KEY=gsk_your_groq_api_key

# OpenRouter API Key - Access to multiple AI models (Claude, GPT-4, etc.)
# Get from: https://openrouter.ai/keys
# Used for: Complex reasoning tasks, fallback when Groq unavailable
OPENROUTER_API_KEY=sk-or-your-openrouter-api-key

# =============================================================================
# N8N WORKFLOW AUTOMATION
# =============================================================================

# N8N Webhook Secret for authenticating incoming webhook requests
# Generate a secure random string (e.g., using: openssl rand -hex 32)
N8N_WEBHOOK_SECRET=your-n8n-webhook-secret

# N8N Bot User ID - UUID of the system bot user for automated actions
# This user should exist in your Supabase users table
# Used to attribute automated content creation/updates to a system account
N8N_BOT_USER_ID=00000000-0000-0000-0000-000000000000

# =============================================================================
# CLOUDFLARE R2 STORAGE (Media/Assets)
# =============================================================================
# Get these from: https://dash.cloudflare.com/ -> R2 -> Manage API Tokens

# Your Cloudflare Account ID
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id

# R2 Access Key ID
CLOUDFLARE_ACCESS_KEY_ID=your-r2-access-key-id

# R2 Secret Access Key
CLOUDFLARE_SECRET_ACCESS_KEY=your-r2-secret-access-key

# R2 Bucket name for media storage
CLOUDFLARE_R2_BUCKET=yokk-media

# =============================================================================
# POSTHOG ANALYTICS
# =============================================================================
# Get these from: https://app.posthog.com/project/settings

# PostHog Project API Key (safe for client-side)
# NEXT_PUBLIC_POSTHOG_KEY=phc_your_posthog_project_key

# PostHog Host (optional - defaults to https://app.posthog.com)
# For EU region: https://eu.posthog.com
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# =============================================================================
# OPTIONAL: ADDITIONAL SERVICES
# =============================================================================

# OpenAI API Key (if using OpenAI directly instead of via OpenRouter)
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-openai-api-key

# Upstash Redis (for caching/rate limiting)
# Get from: https://console.upstash.com
# UPSTASH_REDIS_URL=https://your-redis.upstash.io
# UPSTASH_REDIS_TOKEN=your-upstash-token

# Turso Database (if using edge SQLite)
# Get from: https://turso.tech
# TURSO_DATABASE_URL=libsql://your-db.turso.io
# TURSO_AUTH_TOKEN=your-turso-auth-token

# =============================================================================
# DEVELOPMENT/CI SETTINGS
# =============================================================================

# Node environment (development, production, test)
NODE_ENV=development

# Set to 'true' in CI environments
# CI=true
